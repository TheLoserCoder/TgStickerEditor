export enum SettingsKey {
  THEME = 'theme',
}

export enum SettingsStoreKey {
  SETTINGS = 'settings',
}

export enum SettingsValidationError {
  INVALID_THEME = 'Invalid theme',
}

export enum DefaultTheme {
  SYSTEM = 'system',
}

export enum SettingsDomain {
  SETTINGS = 'settings',
}
