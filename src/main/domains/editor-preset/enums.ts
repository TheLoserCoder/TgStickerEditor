export enum PresetValidationError {
  EMPTY_NAME = 'Название пресета не может быть пустым',
  INVALID_SETTINGS = 'Некорректные настройки пресета',
}

export enum PresetStoreKey {
  PRESETS = 'editorPresets',
}

export enum PresetDomain {
  EDITOR_PRESET = 'editorPreset',
}
