export enum GridValidationError {
  INVALID_COLUMNS = 'Invalid columns count',
  INVALID_CELLS = 'Invalid cells structure',
  FRAGMENT_NOT_FOUND = 'Fragment not found in layout',
  GROUP_NOT_FOUND = 'Group not found in layout',
  POSITION_OUT_OF_BOUNDS = 'Position is out of grid bounds'
}
