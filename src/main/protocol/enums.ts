export enum ProtocolScheme {
  STICKER_PACKS = 'sticker-packs'
}

export enum ProtocolError {
  INVALID_URL = 'Invalid protocol URL',
  FILE_NOT_FOUND = 'File not found',
  HANDLER_ERROR = 'Protocol handler error'
}
