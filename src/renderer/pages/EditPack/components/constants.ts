export const CANVAS_CONSTANTS = {
  SIDEBAR_WIDTH: 300,
  PADDING: 80,
  VIEWPORT_PADDING: 20,
  GALLERY_HEIGHT: 200,
  ZOOM_SCALE_DIVISOR: 100,
  MIN_SCALE: 1,
} as const;

export const CANVAS_LABELS = {
  IMAGE_ALT: 'editor',
} as const;

export const GRID_CONSTANTS = {
  STROKE_WIDTH: 1,
} as const;
